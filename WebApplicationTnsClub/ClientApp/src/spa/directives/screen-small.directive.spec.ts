import { ScreenSmallDirective } from './screen-small.directive';

describe('ScreenSmallDirective', () => {
  it('should create an instance', () => {
    const directive = new ScreenSmallDirective();
    expect(directive).toBeTruthy();
  });
});
